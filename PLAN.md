# PLAN.md — Packup! (M0–M2)

File-by-file plan for the first three milestones. **Awaiting approval before M1.**

Location: `/home/mobellei/Desktop/Games/packup/` — its own git repo (see §0).

---

## 0. Blockers & decisions needing your input

These are the things I cannot resolve on my own. Everything else below I will
just do.

### B1 — This is not a git repository

`/home/mobellei/Desktop/Games` is **not** under version control (`git log` →
`fatal: not a git repository`). The spec requires conventional commits and a
GitHub Actions deploy pipeline, so `packup/` needs to be its own repo. I will
`git init` inside `packup/` and commit there, leaving the rest of your Godot
workspace untouched.

### B2 — No GitHub remote, and `gh` CLI is not installed

M0's acceptance is _"live on GitHub Pages"_. I can produce every artifact —
workflow file, manifest, icons, a build that is verified to work from a
subpath — but I **cannot create the remote or enable Pages** without either the
`gh` CLI authenticated or you creating an empty repo and giving me the URL.

**What I need from you:** create an empty GitHub repo (no README/license) and
paste the URL. Then I run `git remote add` + `git push -u origin main`, and you
flip **Settings → Pages → Source: GitHub Actions** (one click, only you can do
it). Until then M0 ends "green locally, verified via a static file server on a
subpath" and I will say exactly that rather than claim it deployed.

### B3 — Node 18 caps some dependency versions

You are on **Node 18.20.8**. Vite 7 and `vite-plugin-pwa` 1.x need Node 20+.
I will pin **Vite 6 / Vitest 2 / vite-plugin-pwa 0.21**, all Node-18-compatible,
and pin CI to Node 20 so it matches the modern toolchain. Every other pinned
choice in §2 of the spec is unaffected. If you would rather upgrade Node
locally (nvm), say so and I will use the latest versions instead.

### B4 — App name

"Packup!" is marked _working name_. The name is baked into the manifest, the
`id`, and later `STORE.md`. I will ship M0 with `Packup!` /
short_name `Packup` and change it later if you pick another — cheap to change
now, annoying after store submission.

---

## 1. Milestone 0 — Skeleton on the internet

**Goal:** an installable, deployable shell with a placeholder Trips screen, and
a green CI pipeline. No product logic.

### Scaffold & config

| File                                                          | Purpose                                                                                                                              |
| ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| `package.json`                                                | Scripts: `dev`, `build`, `preview`, `typecheck`, `lint`, `format`, `test`, `test:ui`. Deps pinned per B3.                            |
| `vite.config.ts`                                              | `base: './'` (C2), `vite-plugin-pwa` with `registerType:'autoUpdate'`, `injectRegister:'auto'`, full precache glob, manifest inline. |
| `tsconfig.json` / `tsconfig.node.json`                        | `strict: true`, `noUncheckedIndexedAccess`, `noUnusedLocals`, path alias `@/* → src/*`.                                              |
| `tailwind.config.ts` / `postcss.config.js`                    | Tailwind scanning `src/**`. Colors reference CSS vars, not literals.                                                                 |
| `.eslintrc.cjs`, `.prettierrc`, `.editorconfig`, `.gitignore` | Lint/format, run in CI.                                                                                                              |
| `index.html`                                                  | `viewport-fit=cover` (required for C7), theme-color meta, apple-touch-icon link.                                                     |
| `public/404.html`                                             | Copy of `index.html` (spec §6 belt-and-braces).                                                                                      |
| `public/manifest.webmanifest`                                 | Generated by the plugin; `start_url:'.'`, `scope:'.'`, `id`, portrait, standalone, `categories:['travel','utilities']`.              |

### Source

| File                        | Purpose                                                                                                                                  |
| --------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `src/main.tsx`              | Mount, import global CSS, register SW **only when not native** (spec §6).                                                                |
| `src/index.css`             | Tailwind layers + design tokens as CSS variables (one file, per §2), incl. `--safe-top/right/bottom/left` from `env(safe-area-inset-*)`. |
| `src/app/router.tsx`        | `createHashRouter` (C2), routes `/` and `/trip/:tripId`.                                                                                 |
| `src/app/AppShell.tsx`      | Layout shell honouring safe-area insets, min 16px edge padding (C7).                                                                     |
| `src/app/ErrorBoundary.tsx` | Route-level error element.                                                                                                               |
| `src/screens/Trips.tsx`     | Static placeholder list + empty state.                                                                                                   |
| `src/screens/TripView.tsx`  | Placeholder.                                                                                                                             |
| `src/platform/index.ts`     | The `Platform` interface exactly as spec §7.                                                                                             |
| `src/platform/web.ts`       | `navigator.vibrate` / `navigator.share` / `<input type="file" capture>`, `isNative:false`.                                               |
| `src/platform/native.ts`    | Stub throwing "wired in M7", selected by a build-time flag. Keeps M7 configuration-not-surgery.                                          |
| `src/store/ui.ts`           | Empty zustand store with the shape selection/drawer/cameraTarget will take.                                                              |

### Icons (generated, not placeholders — spec §6)

`scripts/generate-icons.mjs` renders one 1024×1024 SVG source
(`assets/icon-source.svg`, a suitcase mark in the accent colour) to
`public/icons/`: 192, 512, 512-maskable (with the 40% safe zone), 180
apple-touch, and `favicon.ico`. Committed as real files. Same source feeds
`@capacitor/assets` in M7.

### CI — `.github/workflows/deploy.yml`

Push to `main` → Node 20 → `npm ci` → `typecheck` → `lint` → `test` → `build`
→ `actions/upload-pages-artifact` → `actions/deploy-pages`. Correct
`permissions: {contents:read, pages:write, id-token:write}` and a `concurrency`
group.

### How I verify M0 (not "it should work")

1. `npm run build` succeeds; `typecheck`, `lint`, `test` all green — I paste output.
2. Serve `dist/` **under a subpath** (`http://localhost:8080/packup/`) and load
   it headlessly; assert no 404s on assets and the placeholder renders. This is
   the real test of `base:'./'` + `HashRouter`.
3. Confirm no root-absolute `/assets/` strings in `dist/index.html`.
4. Screenshot the placeholder at 390×844 to eyeball safe-area padding.
5. Deployment itself is blocked on **B2** and I will report it as blocked, not done.

---

## 2. Milestone 1 — Domain + data

`domain/` is pure: no React, no browser globals, runs under Node (spec §8).

| File                     | Contents                                                                                                                                                                                                                                                                                                             |
| ------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/domain/types.ts`    | Verbatim from spec §3.                                                                                                                                                                                                                                                                                               |
| `src/domain/volume.ts`   | `SIZE_UNITS {small:1,medium:3,large:8}`, `DEFAULT_CAPACITY` per subtype (all 13 values from §5), `usedUnits(items)`, `fillRatio`, `fillStatus() → 'ok'                                                                                                                                                               | 'amber' | 'red'` at the 100%/120% thresholds. Never blocks. |
| `src/domain/rules.ts`    | Invariant checks returning a discriminated `Ok/Err` result rather than throwing, so both the UI and the repo can use one code path: container caps (3/3/3/1), `slotIndex` uniqueness per `(travellerId, kind)`, `nextFreeSlot()`, parent-nesting validity (suitcase/bag only, same traveller, parent has no parent). |
| `src/domain/location.ts` | `resolveLocation(item, ctx) → string[]` breadcrumb, `['Unassigned']` when `containerId === null`.                                                                                                                                                                                                                    |
| `src/domain/index.ts`    | Barrel.                                                                                                                                                                                                                                                                                                              |

| File                    | Contents                                                                                                                                                                                                                                                                                                     |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/data/db.ts`        | Dexie subclass, v1 schema, indexes on `tripId`, `travellerId`, `containerId`, `[travellerId+kind]`. Migration hooks in place from v1 so v2 is routine.                                                                                                                                                       |
| `src/data/repo.ts`      | **The only Dexie consumer** (C3). Interface `Repo` + `DexieRepo` implementation. Enforces §3 invariants — the API refuses a 4th suitcase, not just the UI (acceptance criterion). Cascade semantics: delete container → unassign items; delete traveller → delete containers, unassign items. Transactional. |
| `src/data/backup.ts`    | `exportTrip`/`importTrip` JSON with `schemaVersion`; import validates and remaps UUIDs to avoid collisions.                                                                                                                                                                                                  |
| `src/data/seed.ts`      | The realistic demo trip (spec §4.1: "this is what a store reviewer sees first").                                                                                                                                                                                                                             |
| `src/screens/Debug.tsx` | Dev-only route exercising the repo. Removed by M2.                                                                                                                                                                                                                                                           |

**Tests** (`src/domain/*.test.ts`, `src/data/repo.test.ts` on `fake-indexeddb`):
caps refuse the 4th of each kind; slot uniqueness; nesting depth rejected;
breadcrumb for a nested pouch returns `Pouch → Suitcase`; fill thresholds at
exactly 100% and 120%; delete-container unassigns rather than deletes;
export→wipe→import round-trips to an identical state. Target 100% on `domain/`.

---

## 3. Milestone 2 — Trips & travellers CRUD

| File                                                                                                    | Purpose                                                                                                                                    |
| ------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/components/ui/{Button,IconButton,Card,Input,Field,Sheet,Toast,ConfirmDialog,ProgressRing,Fab}.tsx` | Primitives. `Sheet` is the shared bottom-sheet used by every later flow — built once here, tap-first (C5), focus-trapped, safe-area aware. |
| `src/components/TripCard.tsx`                                                                           | Name, dates, coloured-initial avatars, `packed/total` progress ring.                                                                       |
| `src/components/TripFormSheet.tsx`                                                                      | Create/rename: name, destination, date range.                                                                                              |
| `src/components/TravellerFormSheet.tsx`                                                                 | Name, accent colour swatches, `isSelf`.                                                                                                    |
| `src/components/TripMenu.tsx`                                                                           | Rename, duplicate (copies containers+items, resets `packed`), archive, delete (confirm), export JSON, import JSON.                         |
| `src/screens/Trips.tsx`                                                                                 | Real screen: `useLiveQuery` list, FAB, empty state with one-tap **"Create a sample trip"** → `seed.ts`.                                    |
| `src/screens/TripView.tsx`                                                                              | Traveller tab strip + add-traveller button; 3D area is a labelled placeholder until M4.                                                    |
| `src/store/ui.ts`                                                                                       | Toast queue, sheet stack, undo buffer.                                                                                                     |

Persisted entities stay in Dexie and are read through `useLiveQuery`; zustand
holds only UI/session state (spec §2).

**Verified by:** Vitest for the duplicate/export/import logic, plus a scripted
headless pass through create-trip → add-2-travellers → export → wipe → import,
with screenshots at 390×844. I will show the screenshots.

---

## 4. Conventions from the start

- `DECISIONS.md` created in M0 with B3 (Node 18 version pins) as its first entry;
  every dependency added gets a line (spec §12).
- Conventional commits, one per logical step: `chore(m0): scaffold vite+ts+tailwind`,
  `feat(domain): container cap rules`, …
- After each milestone: commit, then a short summary of what changed and
  anything I decided differently and why.
- Spec §11 out-of-scope list respected; `repo` interface and `updatedAt` left as
  the sync seam, with nothing built on it.

---

## 5. What I am explicitly not doing in M0–M2

3D (M4), items and the drawer (M5), Capacitor packages (M7 — but
`src/platform/` exists from M0 as required), Playwright (M6), and any
`localStorage` use for primary data.

---

## Approval checklist

Before I start M1, please confirm:

1. **B2** — GitHub repo URL, or tell me to defer deployment and keep M0 local.
2. **B3** — Vite 6 pins on Node 18 (default), or you upgrade Node and I use latest.
3. **B4** — name stays "Packup!" for now?
4. The M0–M2 file plan above looks right.
